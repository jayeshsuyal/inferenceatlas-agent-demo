# InferenceAtlas Agent dependencies

# .env loading
python-dotenv>=1.0.0

# LLM inference (OpenAI-compatible SDK — Nebius or OpenAI)
openai>=1.0.0,<3.0.0

# Tavily search
tavily-python>=0.3.0

# Composio tool integrations
composio-openai>=0.6.0

# Web UI
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# OpenClaw agent runtime
# Install from: pip install openclaw
# (falls back to built-in loop if not installed)
# openclaw>=0.1.0
